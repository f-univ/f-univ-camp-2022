import styled from '@emotion/styled';

const Footer = () => {
  return <Wrapper>F.univ</Wrapper>;
};

export default Footer;

const Wrapper = styled.footer`
  padding: 5rem 0 3rem 0;
  text-align: center;
  opacity: 0.8;
`;
